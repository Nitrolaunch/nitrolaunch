{
	"name": "Archive",
	"description": "Clean up old files",
	"protocol_version": 3,
	"hooks": {
		"subcommand": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_archive${EXE_EXTENSION}"
		}
	},
	"subcommands": {
		"archive": "Remove assets that aren't needed anymore"
	}
}