{
	"name": "Args",
	"description": "Use argument presets to increase game performance",
	"protocol_version": 3,
	"hooks": {
		"modify_instance_config": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_args${EXE_EXTENSION}"
		}
	}
}