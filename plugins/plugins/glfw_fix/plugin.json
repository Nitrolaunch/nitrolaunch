{
	"name": "GLFW Fix",
	"description": "Fixes crashes from a bad GLFW version",
	"protocol_version": 3,
	"hooks": {
		"on_instance_setup": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_glfw_fix${EXE_EXTENSION}"
		}
	}
}