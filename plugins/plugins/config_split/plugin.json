{
	"name": "Config Split",
	"description": "Split configuration into multiple files",
	"protocol_version": 3,
	"hooks": {
		"add_instances": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_config_split${EXE_EXTENSION}"
		},
		"add_templates": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_config_split${EXE_EXTENSION}"
		}
	}
}