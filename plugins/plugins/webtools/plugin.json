{
	"name": "WebTools",
	"description": "Launch online Minecraft tools",
	"protocol_version": 3,
	"hooks": {
		"subcommand": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_webtools${EXE_EXTENSION}"
		},
		"add_sidebar_buttons": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_webtools${EXE_EXTENSION}"
		},
		"get_page": {
			"executable": "${PLUGIN_DIR}/nitro_plugin_webtools${EXE_EXTENSION}"
		}
	},
	"subcommands": {
		"webtool": "View and launch web tools"
	}
}