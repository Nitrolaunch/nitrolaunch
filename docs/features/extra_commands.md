# Extra Commands

On Unix systems, you are able to add additional commands to the command line to improve your experience. Simply call `source <nitro_data_dir>/internal/commands.sh`, replacing `<nitro_data_dir>` with the path to your data directory.

## Commands

- `nitro-instance-cd <instance> and nitro-inst-cd <instance>`: Easily change the working directory to the given instance
