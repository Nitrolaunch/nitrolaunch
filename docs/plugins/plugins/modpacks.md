# Modpacks
ID: `modpacks`

The Modpacks plugin lets you import and play modpacks from a few different formats.

## Supported Formats
- `.mrpack`: Modrinth modpacks

## Usage

### Import
Use the `nitro instance import` command along with the `mrpack` format to import an instance. You must specify whether it is a client or server with `--side`.
