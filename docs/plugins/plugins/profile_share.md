# Profile Share
ID: `profile_share`

The Profile Share plugin makes it easy to import and export profiles to share with friends 

## Usage
- `nitro profile share <profile>`: Exports a profile online and gives you a code to copy to share it
- `nitro profile use <code> <id>`: Imports a profile from the given code, giving it <id> as it's new ID in your config
