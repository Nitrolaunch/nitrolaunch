# Paper
ID: `paper`

The Paper plugin allows you to install the Paper and Folia server runtimes on your server instances.

## Usage
Simply use `paper` or `folia` as the loader selection on the instance you want.
