# MultiMC Transfer
ID: `multimc_transfer`

The MultiMC Transfer plugin is used to import instances from MultiMC and PrismLauncher.

## Usage
Use the `nitro instance import` and `nitro migrate` commands along with the `multimc` and `prism` formats.
